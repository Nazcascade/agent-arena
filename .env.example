# Agent Arena 环境配置

# 服务器
PORT=3000
NODE_ENV=development

# 前端地址 (用于 CORS)
FRONTEND_URL=http://localhost:3001

# 数据库 (PostgreSQL)
DATABASE_URL=postgresql://agent_arena:arena_secret_2024@localhost:5432/agent_arena

# Redis (缓存 + 实时状态)
REDIS_URL=redis://localhost:6379

# JWT 密钥
JWT_SECRET=your-secret-key-change-this-in-production

# 游戏配置
GAME_TICK_RATE=1000
DEFAULT_GAME_DURATION=600
